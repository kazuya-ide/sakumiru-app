-- ============================================================
-- 旧版 seed.sql は削除済み（INC-004）
--
-- 【経緯】
-- - 旧 seed.sql は projects.status / start_date / end_date / budget など
--   既に DROP / リネームされた旧スキーマのカラムを参照しており、
--   `supabase db reset` の最後で常時失敗 → CI 赤の原因になっていた。
-- - 同等以上のテストデータは
--     supabase/migrations/20260427000011_seed_test_data.sql
--   に移管済（案件10件 / 顧客5社 / 作業場所10 / 先方担当者5 / 案件種別4）
-- - 二重管理は事故の元なので、本ファイルは空のスタブとして残す。
--
-- 【今後 seed を追加したい時】
-- - 「migration として残したいデータ」→ supabase/migrations/ に SQL 追加
-- - 「ローカル限定の試行データ」→ 個人の psql スニペットで管理し、
--   このファイルにはコミットしない（CI を再び汚染しないため）
-- - 詳細は docs/incident-log.md INC-004 参照。
-- ============================================================

-- 何も実行しない（コメントのみで OK）
